#include <stdio.h>
#include <string.h>
#include <math.h> 

void main(){
	boolean isOver = false;
	int i = 1, currNum = 0;
	while(isOver == false){
		currNum = currNum + i;
		i++;
		
	}
	int result = 0;
	result = 0;
	printf("\nThe value of the first triangle number to have over %i divisors is: %i", inputDiv, result);
}
